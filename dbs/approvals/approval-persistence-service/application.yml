---
### Live profile
spring:
  profiles: live
  datasource:
    driver-class-name: ${spring.datasource.driver-class-name}
    username: ${spring.datasource.username.approval-persistence-service}
    password: ${spring.datasource.password.approval-persistence-service}
    url: ${spring.datasource.url.approval-persistence-service}

